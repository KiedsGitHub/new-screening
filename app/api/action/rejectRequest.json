{
  "name": "update_status",
  "module": "dbupdater",
  "action": "update",
  "options": {
    "connection": "db",
    "sql": {
      "type": "update",
      "table": "request_form",
      "condition": "request_id = :P1",
      "values": [
        {
          "table": "request_form",
          "column": "status",
          "type": "text",
          "value": "Rejected"
        }
      ],
      "params": [],
      "returning": "request_id",
      "query": "update `request_form` set `status` = ? returning `request_id`"
    }
  },
  "meta": [
    {
      "name": "affected",
      "type": "number"
    }
  ]
}